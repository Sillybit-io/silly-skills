## Prompt

> Codify this repo's conventions into AGENTS.md. There is no AGENTS.md yet, so generate one from what is actually here.

## Without skill

An agent with no codification discipline still produces a readable file. It skims the tree, correctly recognises a skills catalogue with a Bun validator behind it, and writes rules that sound right for such a repository. The prose below is competent and most of the individual statements are defensible in the abstract. What the file is not is evidence: not one line points at a place in this repository, so a reader cannot check a single claim — and neither could the agent that wrote it.

````markdown
# AGENTS.md

## Project overview

silly-skills is a catalogue of AI skills. Each skill lives in `skills/<category>/<name>/SKILL.md`. A Bun script validates the catalogue.

## Coding conventions

- Use consistent naming across the repository. Skill names and directory names should be lowercase and hyphenated.
- Write clear, descriptive frontmatter for every skill so agents can find it.
- Keep documentation up to date as the code changes.
- Prefer small, focused functions with descriptive names.
- Use TypeScript's type system fully and avoid `any`.
- Add tests for new functionality.
- Follow Conventional Commits for commit messages.
- Format shell examples in fenced code blocks tagged `bash`.
- Keep line lengths reasonable and prose concise.

## Testing

Run `bun test` before committing. Run `bun run validate` to check the catalogue.
````

Measured against what this repository actually does, that draft fails in six ways:

- Not one rule carries a citation. The bar is two `path:line` references per rule, drawn from two different files, and nothing here clears it.
- "Prefer small, focused functions", "avoid `any`", and "keep line lengths reasonable" are imported best practice. No file was opened to produce them. The last one contradicts the repository's own configuration: `.markdownlint.jsonc` sets `"MD013": false`, so long lines are a deliberate choice here, not an oversight.
- "Add tests for new functionality" is aspirational. The repository holds exactly one test file, `scripts/validate.test.ts`, so the observed behaviour is the opposite of the stated rule. A future agent reads this as a standing instruction and writes tests nobody asked for, in a layout nothing here establishes.
- "tagged `bash`" silently picks a side of a split. `README.md` tags six shell fences `bash`; two skill files tag fourteen `sh`. The draft names the side with fewer files and fewer occurrences, mentions no disagreement, and hands the next agent a documented reason to rewrite fourteen working fences.
- The patterns every one of the nine skills follows — the `Use <skill-name> when you:` stem, the `MUST:` and `NEVER:` pair, the QA-checklist lead-in sentence, the four-backtick skeleton fence, the lint comment above the footer — are absent. The rules that would actually keep an agent's output indistinguishable from the existing catalogue are the ones missing.
- Nothing marks which bytes are generated. The next run either duplicates the file or overwrites whatever a human added in the meantime.

## With skill

Following conventions-codifier against the same repository produces rules a reader can check, a split left open, and two candidates dropped on the record for thin evidence. `AGENTS.md` does not exist, so the skill creates it, writes the generated-block notice above the start marker where later runs cannot touch it, and writes the block below.

````markdown
<!-- This block is generated by conventions-codifier and is replaced on each run. -->

<!-- conventions-codifier:start -->
<!-- Generated from observed repository patterns. Edits inside this block are replaced on the next run. -->

## Conventions (observed)

Sampled 13 files across 6 directories on 2026-09-24. Every rule below is followed by at least two places in this repository that show it.

### Skill document structure

- The "When to use / when NOT to use" section is two bullet lists, opened by the stems `Use <skill-name> when you:` and `Do NOT use <skill-name> when you:`. All 9 skills do this. — `skills/review/ai-review/SKILL.md:18`, `skills/docs/doc-cleanup/SKILL.md:26`
- The Guardrails section is two labelled lists, `MUST:` first and `NEVER:` second, and each bullet repeats its own label as its first word. All 9 skills do this. — `skills/review/ai-review/SKILL.md:175`, `skills/docs/doc-cleanup/SKILL.md:158`
- The QA checklist section opens with one lead-in sentence beginning `Run this list before you`, sitting between the heading and the checkboxes. All 9 skills do this. — `skills/review/ai-review/SKILL.md:205`, `skills/docs/doc-cleanup/SKILL.md:174`
- A markdown skeleton inside Output format is fenced with four backticks and tagged `markdown`, which keeps three-backtick fences usable inside it. — `skills/workflow/conventions-codifier/SKILL.md:108`, `skills/workflow/secret-and-privacy-sweep/SKILL.md:85`
- The attribution footer is immediately preceded by a `markdownlint-disable-next-line MD034` comment. — `skills/review/ai-review/SKILL.md:225`, `skills/docs/doc-cleanup/SKILL.md:193`

### TypeScript under `scripts/`

- Module-scope functions are written as named `function` declarations rather than assigned to a const. — `scripts/validate.ts:194`, `scripts/validate.test.ts:20`
- Each file opens with a file-level JSDoc block stating what the file is and how it is run. — `scripts/validate.ts:2`, `scripts/validate.test.ts:1`

### Contradictions to resolve

1. The info string on shell code fences. 2 files use `sh`, 14 fences in total (`skills/review/ai-review/SKILL.md:87`, `skills/review/review-response/SKILL.md:63`); 1 file uses `bash`, 6 fences in total (`README.md:20`, `README.md:26`). Possible cause, unconfirmed: a boundary between reader-facing repository documentation and skill files, rather than a disagreement. Not resolved here.

### Insufficient evidence

- Section banner comments of the form `/* -- name -- */` separating regions of a source file — found 6 times in 1 file, `scripts/validate.ts`; `scripts/validate.test.ts` carries none. Six occurrences inside one file is one author's habit in one sitting, not two files agreeing. Not stated as a rule.
- A `.test.ts` suffix with the test file beside its subject — found 1 time, `scripts/validate.test.ts`, which is the only test file in the repository. Below the two-occurrence bar. Not stated as a rule.

<!-- conventions-codifier:end -->
````

Then the run report, handed back rather than written into the repository:

````markdown
## Conventions codified — AGENTS.md

- Target: `AGENTS.md`. The repository root had no `AGENTS.md` and no reservation on the name, so no fallback to `CONVENTIONS.md` was needed.
- Sample: 13 files across 6 directories — `skills/review`, `skills/docs`, `skills/ai-health`, `skills/workflow`, `scripts`, and the repository root. Excluded: `node_modules/`, `.omo/`, `reports/`, and `*.log`, all four listed in `.gitignore`.
- Rules written: 7. Contradictions listed: 1. Candidates dropped for thin evidence: 2.
- Block action: created the file, wrote the one-line generated-block notice above the start marker, then wrote the block.
- Outside the block: 0 bytes before, 86 bytes after. The file did not exist, so the only content outside the block is the notice line this run wrote. No pre-existing byte was touched.
- Re-run check: a second run changed nothing. The sample, the seven rules, the contradiction, and the two dropped candidates came back identical.
- Disagreements with existing human-written text: none. `AGENTS.md` did not exist before this run.
````

Three differences matter more than the rest. The contradiction is reported rather than settled: with both counts in front of them the team can pick `sh` or `bash` in a minute, whereas a rule that picked for them would have sent the next agent to rewrite the losing side with a documented mandate. The banner-comment candidate is dropped despite six occurrences, because all six sit in one file — the two-citation bar asks for two files that agree, and frequency inside a single file cannot substitute for that. And "Add tests for new functionality", the rule in the unguided draft a reader would most likely trust, never appears at all, because one test file is not a pattern and nothing in this repository states the rule.
